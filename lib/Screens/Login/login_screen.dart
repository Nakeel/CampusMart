import 'package:flutter/material.dart';
import 'package:campus_mart/Screens/Login/components/body.dart';

class LoginScreen extends StatelessWidget {
  static const String tag = 'log-in';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
      
    );
  }
}

